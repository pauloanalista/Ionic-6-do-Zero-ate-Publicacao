import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.ilovecode.todoionic6',
  appName: 'Todo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
